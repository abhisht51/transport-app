import 'package:flutter/material.dart';

class ToSNU extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("This is From SNU PAGE"),
    );
  }
}