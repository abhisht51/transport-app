import 'package:flutter/material.dart';

class FromSNU extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("TO SNU PAGE"),
    );
  }
}